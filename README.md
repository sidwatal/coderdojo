# CoderDojo NYC

**Setup instructions...**

    $ vagrant up
    $ vagrant ssh
    $ bundle install
    $ rake db:create
    $ rake db:migrate
    $ rails s -b 0.0.0.0

**Run on Local Browser**

http://localhost:3000
